## 2024-05-23 - [Lazy Loading in jQuery SPA]
**Learning:** SPA via jQuery visibility toggling (display: none) means 'hidden' images are eagerly loaded by default.
**Action:** Use `loading='lazy'` on all off-screen images, even those in hidden divs, to prevent unnecessary bandwidth usage.
