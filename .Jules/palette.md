## 2024-05-22 - Accessibility Quick Wins
**Learning:** Adding `aria-label`, `role="button"`, and `tabindex="0"` to interactive `div` elements and icon-only links significantly improves accessibility with minimal code changes.
**Action:** Always check for `div`s used as buttons and add appropriate semantic roles and keyboard handlers.
